{% extends "base.html.twig" %}



{% block title %}{{ c[47]|raw }} | {% endblock %}



{% block body %}

    {% include "nav.html.twig" %}

    <div class="standard-content">
        <div class="ui text container">
            <h1 class="ui horizontal divider huge header">{{ c[47]|raw }}</h1>

            <div class="part-of-org-box">
                <div class="org-box fyeg">
                    <img src="/assets/images/FYEG-logo-full.png" alt="Logo of Federation of Young European Greens"/>
                    <div class="description">
                        {% if lang is same as('lv') %}
                            Kopš 2020. gada 29. augusta "Protests" ir <a href="https://fyeg.org/" target="_blank">Eiropas jauno zaļo federācijas (FYEG)</a> dalīborganizācija.
                        {% endif %}
                        {% if lang is same as('en') %}
                            Since August 29, 2020, "Protests" is a member organization of the <a href="https://fyeg.org/" target="_blank">Federation of Young European Greens</a>.
                        {% endif %}
                        {% if lang is same as('ru') %}
                            С 29 августа 2020 "Protests" членская организация <a href="https://fyeg.org/" target="_blank">Федерации молодых европейских зеленых (FYEG)</a>.
                        {% endif %}
                    </div>
                </div>
                <div class="org-box ljp">
                    <img src="/assets/images/LJP-logo.png" alt="Latvijas jaunatnes padomes logo"/>
                    <div class="description">
                        {% if lang is same as('lv') %}
                            Kopš 2019. gada 2. novembra "Protests" ir <a href="http://ljp.lv/" target="_blank">Latvijas Jaunatnes padomes</a> kandidātdalīborganizācija.
                        {% endif %}
                        {% if lang is same as('en') %}
                            Since November 2, 2019, "Protests" is an associeted member of <a href="http://ljp.lv/" target="_blank">National Youth Concil of Latvia (LJP)</a>.
                        {% endif %}
                        {% if lang is same as('ru') %}
                            С 2 ноября 2019 "Protests" ассоциированный член <a href="http://ljp.lv/" target="_blank">Латвийского Молодежного совета (LJP)</a>.
                        {% endif %}
                    </div>
                </div>
            </div>

            <h1 class="ui big centered header">{{ c[64]|raw }}</h1>

            <div class="partner-list">
                {% for partner in partners %}
                        {% if partner.link is same as("") %}<div class="partner-logo"
                        {% else %}<a class="partner-logo" href="{{partner.link}}" target="_blank"{% endif %}  data-inverted="" data-tooltip="{{partner.name}}" data-position="top center">
                        <img src="/assets/uploads/{{partner.logo}}"/>
                        {% if partner.link is same as("") %}</div>
                        {% else %}</a>{% endif %}
                {% endfor %}
            </div>

        </div>
    </div>


{% endblock %}



{% block javascripts %}
{% include "navjs.html.twig" %}

<script>

/*$(document).ready(function(){
    $('.owl-carousel').owlCarousel({
        loop:true,
        margin:10,
        nav:true,
        autoplay:true,
        autoplayTimeout:2000,
        autoplayHoverPause:true,
        responsive:{
            0:{
                items:1
            }
        }
    })
});*/

</script>
{% endblock %}